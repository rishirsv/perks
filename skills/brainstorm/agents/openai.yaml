interface:
  display_name: "Brainstorm"
  short_description: "Explore ideas through structured conversation"
  default_prompt: "Explore this idea through structured conversation until the scope, decisions, and tradeoffs are clear."

policy:
  allow_implicit_invocation: false
