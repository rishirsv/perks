interface:
  display_name: "Docs Gardener"
  short_description: "Keep repository docs aligned with code"
  default_prompt: "Audit, update, and reconcile this repo's canonical documentation with the current codebase."

policy:
  allow_implicit_invocation: false
