interface:
  display_name: "Hard Cut"
  short_description: "Remove compatibility code and keep one final shape"
  default_prompt: "Apply a hard-cut policy to this change and remove compatibility, migration, fallback, and dual-shape code."
