interface:
  display_name: "Handoff"
  short_description: "Create a clear continuation handoff"
  default_prompt: "Use $handoff to create a concise continuation brief with current state, files touched, decisions, risks, and the exact next step."
