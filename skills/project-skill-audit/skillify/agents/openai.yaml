interface:
  display_name: "Skillify"
  short_description: "Turn finished work into reusable skills"
  default_prompt: "Turn the work from this session into a reusable skill."

policy:
  allow_implicit_invocation: false
