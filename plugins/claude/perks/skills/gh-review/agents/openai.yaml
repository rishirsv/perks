interface:
  display_name: "GH Review"
  short_description: "Triage, review, address, and merge GitHub PRs"
  default_prompt: "Use $gh-review to triage open PRs: summarize each one simply, explain state and implementation detail, and recommend the next action."

policy:
  allow_implicit_invocation: false
