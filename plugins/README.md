# Plugins

Installable plugin packages live here.

Each plugin owns its manifest at `.codex-plugin/plugin.json` and keeps bundled skills, agents, hooks, assets, `.mcp.json`, and `.app.json` at the plugin root, matching OpenAI's documented plugin structure.
