# Codex Marketplace

`marketplace.json` is the canonical local marketplace for Codex.

The marketplace points at installable plugins under `../../plugins/`, currently `../../plugins/perks`. Keep Codex plugin entries here, not in `.claude-plugin/`.
