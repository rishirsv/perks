# KPMG Slides Skill - Python runtime dependencies
#
# Install from the skill directory:
#   python3 -m pip install -r requirements.txt
#
# These packages power preview rendering, montage generation, and
# visual overflow checks for the bundled postprocess runtime.

pdf2image==1.17.0
Pillow==11.1.0
numpy==2.2.3
python-pptx==1.0.2
