# Primitive Authoring

This folder is the repo-only authoring surface for reusable primitives.

These primitive fragments are authoritative for runtime registry metadata, including built-in slide types.

Edit primitive behavior here, then regenerate runtime aggregates with `npm run onboard:regen`.
